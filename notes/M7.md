# M7 Execute in Claude Code

Branch: `m7-note-search-feature`

## What happened
- Implemented note search on the homepage using a `?q=` query parameter.
- Added a search normalization helper in `app.py` and a search form in `templates/home.html`.
- Added focused acceptance tests in `tests/test_search.py`.

## Flagged issue
- The chat plan did not anticipate that the current `main` branch baseline did not yet include the earlier task 01/02 route behavior, so the repository's existing test suite fails by default on this branch. I verified the new search tests independently.

## Verification
- `./.venv/bin/python -m pytest -q tests/test_search.py` → `5 passed`
- Manual browser check on `http://127.0.0.1:5000` confirmed the search form renders, the note can be created, and searching filters results.
