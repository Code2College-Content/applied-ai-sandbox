# M7 — Multi-Tool Workflows

## Feature I picked
- Note search (free-text filter against title and body)

## Plan from chat
Create a 1 page design no code. For a note taking fullstack app with a python backend, create a search bar to search through notes by title.

The design shows a full-page app layout with two zones: a sidebar and a main content area.
Sidebar — search experience:

The search bar filters notes by title in real time as you type, with match highlighting on the results
A clear (×) button appears when there's text in the field
A results count label updates dynamically ("3 results for 'notes'")
Clicking any note sets it as active (green border indicator)

Main content:

Shows the selected note's title, tags, and a body preview
Includes a callout showing the Python backend endpoint pattern: GET /api/notes?q={title} using a LIKE '%title%' query on SQLite, returning JSON ordered by updated_at

Design direction: editorial/utilitarian — Lora serif for a notepaper feel, JetBrains Mono for metadata and code, teal accents throughout, clean 0.5px borders everywhere.


## Execute in Claude Code
- Branch:    https://github.com/Code2College-Content/applied-ai-sandbox/pull/26
- One thing Claude flagged that the chat plan missed: External JS

## Polish with Copilot
- Two small edits I accepted: Lora Headings, Debounce

## Review in chat
- One useful piece of feedback:
Edge cases not handled

note["owner"] (hard key, not .get()) raises KeyError if a note was written before the owner field existed. Make it note.get("owner").
## Parallel sessions
- Second task: Fix Edge Cases
- Worktree path: N/A
- Honest answer — did I actually parallelize or did one session sit idle? One had to sit idle in order for code to be written and check for edge cases

## Workflow retro
- (200-300 words)

First, Claude Chat planned the code. This included the search bar filtering notes by title in real time as you type with match highlighting on the results, a clear × button appears when there's text in the field, a results count label updates dynamically ("3 results for 'notes'"), and clicking any note sets it as active (green border indicator). Next, code was actually written by pasting the plan with guidelines into Github Copilot. This included a search view, two-column layour, and a JSON array of notes. Copilot autocomplete polished up anything that was small. Finally, parralelisation allowed for multiple tasks at the same time, even though I really just used one to write code and the other to polish it. Github Copilot was able to fill any missing pices specfic to the project due to having access to all of the code, and ClaudeAI futher catched edge cases within with the git diff. If I re-did this feature, I would not use AI, as I would actually want to learn how to do this on my own. I spent most of the time creating prompts, and generally being a guide to the AI to steer it in the right direction.