# M7 — Multi-Tool Workflows

## Feature I picked
- I want to create a feature that allows users to search for their notes. 

## Plan from chat
Interface changes
Search input in notes header
Persistent text field (not a modal/overlay). Clears on Esc. Focus on ⌘K.

Debounced requests (300 ms)
Fire API call after user pauses typing. Cancel in-flight request when new keystroke arrives.

Highlighted snippets in result rows
Show body excerpt with matched terms wrapped in <mark>. Truncate to ~120 chars around first match.

URL reflects query
Sync ?q= to address bar via history.replaceState so searches are shareable and survive reload.

## Execute in Claude Code
- Branch:m7-feature
- One thing Claude flagged that the chat plan missed: Claude didn't miss anything in the chat plan. 

## Polish with Copilot
- Two small edits I accepted: Found the missing form validation path. Updating app.py and templates/new_note.html so invalid submissions show errors and preserve typed values.

## Review in chat
- One useful piece of feedback: Copilot was good at explaining the features that Claude added, and why some features were incorrect. 

## Parallel sessions
Second task - create a new worktree
Worktree - Makes multiple working copies of one repo
Parallelism - I didn't actually parallelize 

## Workflow retro
The feature I just shipped was creating a word search / filter for users to locate files easily. I used Git, Copilot, and Claude. Claude was used to create the feature, Copilot was used to review Claude's code, and Git was used to push. Copilot catched found out that there was a  missing form validation path. So Copilot updated app.py and templates/new_note.html so invalid submissions show errors and preserve typed values. If I re-did this process I would use Copilot before I made changes from Claude so that I can double check my work. I used Claude majority of the time and it was able to make a functional filter/search bar for the notes website. It also provided a detailed explanation/plan and then I used Copilot to write a little bit of code. I believe that Claude was the right conductor for this project because it was able to correctly create this useful feature as well as stick to changing files that were supposed to be changed. Copilot was also great at explaining the issues, however I believe that Claude does a better job at implementing / creating features for websites. 