# M7 — Multi-Tool Workflows

## Feature I picked
Free-text search that filters notes by title and body in real time.

## Plan from chat
Here is the plan I want to implement:

Add free-text search to the homepage. Update the GET / route to accept 
an optional ?q= param and filter notes by title/body. Add a search bar 
to home.html as a GET form. Show "no notes found" if no results.

Constraints:
- Don't change the in-memory note dict shape unless required
- Don't touch existing tests except to add new ones
- Use the same patterns as existing routes (no new frameworks)

Acceptance:
- A GET to / with ?q=flask returns only notes whose title or body contains "flask" (case-insensitive)
- A GET to / with no query param or empty ?q= returns all notes as normal
- A "no notes found" message is shown when the search has zero results

## Execute in Claude Code
- Branch: note-search
- One thing Claude flagged that the chat plan missed: delete uses loop.index0 from the filtered list, but app.notes is unfiltered — deleting from a search result view would remove the wrong note.

## Polish with Copilot
- Two small edits I accepted:

## Review in chat
- One useful piece of feedback:

## Parallel sessions
- Second task:
- Worktree path:
- Honest answer — did I actually parallelize or did one session sit idle?

## Workflow retro
- (200-300 words)