# M4 — Reading and Verifying Output

## Bug hunt #1 — SQL injection
- Bug: The code allows SQL injection by putting the user input directly in the SQL command.
- Why Claude wrote it: Claude most likely wrote it this way to match the pattern of simplified examples, leaving vulnerability.
- Fix: Keep the user input and the SQL command separate so that a user cannot preform a SQL injection.

## Bug hunt #2 — hallucinated package
- Bug: The package flask_simpleauth doesn't actually exist.
- Why Claude wrote it: It most likely combined principles from existing packages to try to fit the user's use case without realizing it hallucinated a new package.
- Fix: Find a working package that will satisfy the user's needs.

## Bug hunt #3 — edge case
- Bug: Claude did not recognize the edge case of inputting an empty string, which tries to divide by zero.
- Why Claude wrote it: Claude simply preformed the task; it needed clarification to handle edge cases.
- Fix: Add a conditional statement to check if it is an edge case like an empty list.

## Bug hunt #4 — hardcoded secret
- Bug: The API key is hard coded into the code, so it can get leaked if it is pushed to a remote repository.
- Why Claude wrote it: It creates the simplest solution without thinking about concerns for real-world application.
- Fix: Place the API key in a separate file with .gitignore applied or make it an environment variable.

## Test I wrote
- Bug I picked: Bug #3 - edge case.
- Test code: def test_claude_average_edge_case():
    assert claude_average_rating([]) == None

def test_fixed_average_edge_case():
    assert fixed_average_rating([]) == None
- Confirmed it fails on the bug and passes on the fix? (yes/no) yes

## Near-miss reflection
- Claude produced a PR change in the sandbox Flask app that completed Task 1, but it also touched a couple of unrelated files and made broader edits than the task actually required. What made me stop and look closer was that the behavior worked, but the scope of changes didn’t match the narrow instructions in the TASK_01.md file. I would have caught it earlier if I knew to check every detail of Claude's work by diffing changes, especially that of important logic or implementations.