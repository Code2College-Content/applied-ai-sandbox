# M4 — Reading and Verifying Output

## Bug hunt #1 — SQL injection
The bug is SQL injection: the code interpolates `q` directly into the SQL string, allowing an attacker to inject SQL via the `q` parameter.
Claude likely copied a simple raw-SQL example and used an f-string because it looked like a direct translation of that example without applying parameter binding or the ORM's safer APIs.
Fix this by using a parameterized query or the ORM's filter methods (for example use `db.session.execute(text("SELECT id, title FROM notes WHERE title LIKE :pattern"), {"pattern": f"%{q}%"})` or `Note.query.filter(Note.title.ilike(f"%{q}%"))`), so user input is never concatenated into SQL.

## Bug hunt #2 — hallucinated package
- Bug:
- Why Claude wrote it:
- Fix:

## Bug hunt #3 — edge case
- Bug:
- Why Claude wrote it:
- Fix:

## Bug hunt #4 — hardcoded secret
- Bug:
- Why Claude wrote it:
- Fix:

## Test I wrote
- Bug I picked:
- Test code:
- Confirmed it fails on the bug and passes on the fix? (yes/no)

## Near-miss reflection
- (3-5 sentences)
Fill it in as you go. Each step below points 