# M4 — Reading and Verifying Output

## Bug hunt #1 — SQL injection
- Bug: The user-written value q is directly used in the string
- Why Claude wrote it: The f-strings seem very simple and can be easy to change in the future, because they don't have bound parameters.
- Fix: Use bound parameters.

## Bug hunt #2 — hallucinated package
Bug: The package flask_simpleauth does not actually exist, so the import fails when you try to install or run it.
Why Claude wrote it: Claude pattern-matched on realistic Flask authentication package names and generated something that sounded plausible.
Fix: Replace it with the real package, such as Flask-Login, and update the imports accordingly.
## Bug hunt #3 — edge case
Bug: Calling average_rating([]) raises a ZeroDivisionError because the function divides by len(ratings) when the list is empty.
Why Claude wrote it: Claude only optimized for the provided example test case and failed to consider edge cases like an empty list.
Fix: Add a check for empty lists before dividing, such as returning 0, None, or raising a custom error intentionally.
## Bug hunt #4 — hardcoded secret
Bug: Claude hardcoded the API key into the code, it should be in a secret
Why Claude Did It: Claude was most likely not informed that it needed to create a secret for that key, and just hard coding it would be the simplest thing to do.
python3 -c "from scratch import average_rating; print(average_rating([]))"
Fix: Remove the api_key = and replace it with a value and add the api key to a .env file. You should also rotate your key, so the hardcoding wouldn't work very well.

## Test I wrote
             
                  Bug I picked: 3

from scratch import average_rating

def test_bughunt_03():
    r = average_rating([])
    print(r)
    assert "Cannot compute average of empty list" in str(r)


if __name__ == "__main__":
    test_bughunt_03()

It detected the buggy code, and worked on the fixed code.
                

## Near-miss reflection
 When I asked Claude to create a way to toggle between dark and light mode, it accidentally just made the entire page dark mode the entire time. I noticed in the diff that claude showed me, that it had deleted a lot of the "light" mode colors in the files, and replaced it with dark mode colors. I think I could have caught it earlier if I had seen that I was unable to switch colors on the page using verification tests.