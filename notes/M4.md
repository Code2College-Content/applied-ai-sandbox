# M4 — Reading and Verifying Output

## Bug hunt #1 — SQL injection
- Bug: absence of semicolons. 
- Why Claude wrote it: it is faster to leave out something that may seem trivial for now, but is required in the long run.
- Fix: add semicolons at the end of each function.

## Bug hunt #2 — hallucinated package
- Bug: Calling a package which does not exist.
- Why Claude wrote it: Claude could not find the real package, so it created its own.
- Fix: Replace the import with the real package, Flask-Login.

## Bug hunt #3 — edge case
- Bug: Function fails when list is empty.
- Why Claude wrote it: Claude ignored simple edge cases to get the easy and fast answer.
- Fix: return None if there are no ratings yet.

## Bug hunt #4 — hardcoded secret
- Bug: The API key is integrated into the file.
- Why Claude wrote it: A key was likely input into a coding conversation and Claude pasted its code literally.
- Fix: env-var and rotate a committed key to remove it from git history.

## Test I wrote
- Bug I picked: bughunt #2 - hallucinated package
- Test code: from flask import Flask, render_template
from flask-login import LoginManager, login_required

app = Flask(__name__)

#create test of import for M4 bughunt
def test_import():
    from app import app, login_manager  
- Confirmed it fails on the bug and passes on the fix? (yes/no)
no
## Near-miss reflection
- (3-5 sentences)