# M4 — Reading and Verifying Output

## Bug hunt #1 — SQL injection
- Bug: Users can gain access to user accounts by using /search?q='; DROP TABLE notes; -- because q is concatenated into user input. 
- Why Claude wrote it: Claude wasn't given any context and therefore used concatenation
- Fix: Use placeholders instead of direct variables in SQL commands

## Bug hunt #2 — hallucinated package
- Bug: WARNING: Package(s) not found: flask_simpleauth.
- Why Claude wrote it: Claude thought it was a legitimate package but it isn't.
- Fix: Replace package with flask-login

## Bug hunt #3 — edge case
- Bug: ZeroDivisionError: division by zero
- Why Claude wrote it: Didn't catch all of the edge cases 
- Fix: If there are no ratings return None

## Bug hunt #4 — hardcoded secret
- Bug: API key is hardcoded into the code. 
- Why Claude wrote it: User gave Claude the API key and Claude used it inside of the code.
- Fix: Avoid giving Claude your API key and  use environment variables

## Test I wrote
- Bug I picked: 
def average_rating(ratings):
    """Return the average of a list of ratings."""
    return sum(ratings) / len(ratings)
assert average_rating([4, 5, 3]) == 4.0

- Test code:
def test_average_rating():
    assert average_rating([4, 5, 3]) == 4.0
    assert average_rating([]) == None  

def test_fixed_average_rating():
    assert fixed_average_rating([4, 5, 3]) == 4.0
    assert fixed_average_rating([]) == None  
- Confirmed it fails on the bug and passes on the fix? (yes)
tests/test_m4_bughunt.py::test_average_rating FAILED                                                                                                                [ 50%]
tests/test_m4_bughunt.py::test_fixed_average_rating PASSED                                                                                                          [100%]


## Near-miss reflection
- (3-5 sentences)
Claude made a few changes to files I didn't want to be changed. I looked closer into it because I saw that these files had changed while I was running Claude. I would have caught this earlier if I regularly check the commits that were being put into VS code. 