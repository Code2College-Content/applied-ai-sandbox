# M4 — Reading and Verifying Output

## Bug hunt #1 — SQL injection
- Bug: Input not paramertarized
- Why Claude wrote it: Claude did not consider SQL injection
- Fix: Tell Claude to add input parameratization or do it yourself

## Bug hunt #2 — hallucinated package
- Bug: flask_simpleauth is not a package
- Why Claude wrote it: AI can hallucinate
- Fix: Tell Claude that package it doesn't exist or fix it yourself mananully

## Bug hunt #3 — edge case
- Bug: Assumes input list has >0 items
- Why Claude wrote it: Did not consider edge case
- Fix: Tell it to fix edge case or check for input manually

## Bug hunt #4 — hardcoded secret
- Bug: Hardcoed OpenAI Key
- Why Claude wrote it: Likely assumed testing environment
- Fix: Create a .env file and add it to a .gitignore

## Test I wrote
- Bug I picked: Edge Case
- Test code:  assert average_rating([]) == 0.0
- Confirmed it fails on the bug and passes on the fix? yes

## Near-miss reflection
- (3-5 sentences)
On M3, we tested a bad prompt. The AI made a basic login/logout system with flask and no hashing. I almost wrote that the AI did nothing wrong other than no password hashing, but then I realized that the login/logout did not do anything after looking through app.py.

I would have caught this earlier if I had actually tested the code or specified the scope.