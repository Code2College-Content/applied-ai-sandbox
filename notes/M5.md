# M5 — Task Decomposition

## Decomposition (8-10 tasks)
1. Make note dictionary shape include tags key (list of strings, default empty). Verify: existing tests still pass; a new added note has tags == [].
2. Update new-note form to accept comma-separated tag field. Verify: creating a note with 'work, urgent' yields note["tags"] == ["work", "urgent"].
3. Update new-note in HTML with tags text input and display. Connect POST handler to save the list. Verify: creating note with works,urgent saves as ["work", "urgent"].
4. Add filter bar in search, each tag linking to /?tag=... Verify: Filtering for certain tags displays posts with those tags.
5. Update home to read ?tag= from query string and filter app.notes before rendering. Verify: / and /?tag= show the same set.
6. Add testing to cover the parser, save path, and filter. Verify: tests pass.

## Task 1 context brief
Goal: Add a `tags` key to the note dict so every note has a tags field
by default.

Constraints:
- tags should be a list of strings, default empty list []
- Don't change anything else about the note dict shape
- Don't touch any templates or routes yet — data layer only
- Existing tests must still pass

Files I expect to change:
- app.py — where notes are appended to the in-memory list

Acceptance:
- A note created without tags has `note["tags"] == []`
- Existing tests in tests/test_task_01.py and tests/test_task_02.py
  still pass after the change

First: read the relevant files and propose a plan in plan mode.
Don't write code yet.

## Task 1 — what happened
- Claude's plan summary: Locate the line in app.py where a new note is appended to the app.notes list.
Add a tags key to the note dictionary with a default value of an empty list ([]).
Ensure no other parts of the code are modified.
Test the change by running the specified test files to confirm no regressions.
- What I pushed back on: It renamed "body" to "content," so I reverted it
- Verification result: Pass

## Task 2 — what happened
- ...

## Task 3 — what happened
- ...

## Reflection — where I redirected
- (3 sentences)