# M5 - Task Decomposition

## Decomposition (6 tasks)
1. **Task 1 — Note dict shape**: Add `tags` key (default `[]`) to each note dict. _Verify: existing tests still pass.
2. **Task 2 — parse_tags helper**: Write a `parse_tags(raw)` helper — splits on commas, strips, lowercases, drops empty. _Verify: unit test on messy input.
3. **Task 3 — Form input**: Add a tags text input to `new_note.html` and wire `parse_tags` into the POST handler. _Verify: submitted tags land on the note dict._
4. **Task 4 — Chip rendering**: Render tags as chips on each note in `home.html`. _Verify: tags appear in page HTML._
5. **Task 5 — Filter**: Add `?tag=` filter logic to the `/` route and a filter bar in `home.html`. _Verify: `GET /?tag=work` returns only matching notes._
6. **Task 6 — Tests**: Write `tests/test_tags.py` covering parse_tags and the filter route. _Verify: full pytest suite stays green._

## Task 1 context brief
Goal: Add a `tags` key to the note dict so the data shape supports tags
from this point forward. No UI yet — just the data layer.

Constraints:
- Notes are stored as dicts in `app.notes` (in-memory list, no database).
- A new note should default to `tags: []` if no tags are provided.
- Don't touch the form, templates, or any other routes yet.
- Don't break existing tests.

Files I expect to change:
- `app.py` only — where notes are appended in the POST handler.

Acceptance:
- A note created via POST has `note["tags"] == []` by default.
- Existing `pytest tests/test_task_01.py` still passes unchanged.

First: read `app.py` and propose a plan. Don't write code yet.

## Task 1 — what happened
- Claude's plan summary: Add `tags: []` to the note dict in the POST handler in app.py.
- What I pushed back on: (anything Claude tried to do beyond that one line)
- Verification result: pytest passed, new note has tags == [].

## Task 2 — what happened
- Claude's plan summary: Add a `parse_tags(raw)` function to app.py that splits on commas, strips whitespace, lowercases, and filters empty strings.
- What I pushed back on: Nothing — the plan matched the brief exactly.
- Verification result: pytest passed; parse_tags("Work, , URGENT") == ["work", "urgent"] and parse_tags("") == [].

## Task 3 — what happened
- Claude's plan summary: Add a tags `<input>` field to `new_note.html` and update the POST handler in app.py to call `parse_tags()` on the submitted value and store the result as `note["tags"]`.
- What I pushed back on: Claude initially tried to update home.html to render chips — pushed back to keep that out of scope for this task.
- Verification result: pytest passed; submitting "work, urgent" stored `note["tags"] == ["work", "urgent"]`, empty submission stored `[]`.

## Reflection — where I redirected
- Task 3 needed an explicit "no changes to home.html" constraint or Claude jumped ahead to render chips. The Task 2 brief added "no UI changes" specifically because Task 1 showed Claude tends to do more than asked. If redoing this from scratch, I'd add a "files I expect to NOT change" line to every brief, not just the ones where scope creep already bit me.