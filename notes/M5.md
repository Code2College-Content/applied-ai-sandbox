# M5 — Task Decomposition

## Decomposition (8-10 tasks)
1. **Task 1 — <name>**: <what changes>. _Verify: <how>._
2. **Task 2 — <name>**: ...
3. ...

## Task 1 context brief
1. Extend note dict structure to include tags key, which should be a list of strings, empty at default. Verify existing tests still pass and new notes show tags == []
2. Update note creation logic to allow comma separated string of tags to be converted to list. Verify that a new note with "tag1, tag2" produces ["tag1", "tag2"]
3. Modify create note page to include an input field for tags. Verify UI shows an input field for tags.
4. Update notes list to display tags next to the notes. Verify that UI displays correct tags on correct notes
5. Add tests for tag parsing including testing for edge cases. Verify tests cover everything for tags and cover edge cases as well
6. Ensure old notes without tags display properly. Verify notes without tags don't crash or display empty slots for tags

## Task 1 — what happened
- Claude's plan summary: Proposed to add tags: [] to the app.notes.append() dict in new_note() and exctact a create_note(title, body, tags=None) helper function to centralize note creation
- The helper function was not needed and should only really be a one-line change
- Verification result: Relevant tests Passed

## Task 2 — what happened
- Claude proposed reading the raw tags string and splitting/stripping inline inside new_note(). It also suggested adding a standalone parse_tags(raw: str) -> list[str] helper above create_app() so the logic stays testable in isolation.
- Again pushed back on helper function
- Verification result: Relevant tests Passed

## Task 3 — what happened
- Claude planned to add the label + input to new_note.html
- No pushback
- Verification result: Relevant tests Passed

## Reflection — where I redirected
- It came back wrong on the first try by trying to add a helper function to support a one-line change. The second task, still tried to add a helper function, which was more justified, as it was a useful function that could have been used for something else. If I re did this, I would make my instructions narrower.