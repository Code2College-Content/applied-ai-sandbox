# M5 — Task Decomposition

## Decomposition (8-10 tasks)
1. Make an external CSS file for css for new_note.html
2. Add the CSS file to a new static folder
3. add css to new_note.html, check if dimensions are correct
4. Include colors sizing and position, check iuf everything fits
5. Make sure CSS is in External File
6. Make sure new_note.html links to the css

## Task 1 context brief
Add CSS to new_note.html.

New CSS file is in a folder named "static". Add Colors, position, and sizing to make it look nice on all screen sizes including mobile and pc. Make sure the CSS is linked in the html.

Existing tests should still pass, and there should be no clipping or sizing issues.
Rread the relevant files and propose a plan in plan mode. Don't write code yet.

## Task 1 — what happened
- Claude's plan summary:

Inspecting templates and files

I need to modify the new_note.html file and add a static CSS file. I should inspect the templates to see if the app uses a static folder, which is likely the case in Flask. Let's check the current new_note.html and possibly existing CSS files. I'll read new_note.html and look at app.py, as well as templates/home quickly. I also want to check the tests to ensure there are no specific assertions that could affect this.
- What I pushed back on: 
UTF encoding character was invalid
- Verification result:
HTML Correct

## Task 2 — what happened
Created a folder

## Task 3 — what happened
Created CSS
All pytests passed

## Reflection — where I redirected
Plan came back wrong because HTML was edited. I needed to manually remove and invalid UTF chararacter, so I was more specific saying that only CSS should be be focused on. If I re-did the whole thing from strach, I would only have the AI do the CSS, and I would implement it with the HTML.