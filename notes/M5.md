# M5 — Task Decomposition

## Task 1 — what happened

* Claude's plan summary: Claude proposed adding a `tags: []` key to the note dictionary in `app.py`.
* What I pushed back on: I did not push back because the plan stayed focused and only modified `app.py`.
* Verification result: I manually ran the app and confirmed notes still save without crashing.

## Task 2 — what happened

* Claude's plan summary: Claude added a tags input field to the new note form and parsed comma-separated tags into a list before storing them.
* What I pushed back on: I did not push back because the plan stayed within scope and did not add filtering.
* Verification result: I confirmed the tags field was added and notes could be saved with tags such as "work, urgent".
