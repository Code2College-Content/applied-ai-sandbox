# M5 — Task Decomposition

## Decomposition (6-10 tasks)
1. **Task 1 — Create Tags**: Add a tags field to each note with a default value of an empty list. Verify: old tests still pass and new notes start with tags = [].
2. **Task 2 — Divide Tags**: Create a helper function to turn a comma-separated string into a clean list of tags. Verify: "work, school, urgent" becomes ["work", "school", "urgent"].
3. **Task 3 — Store Tags**: Add a tags input field to the new note form and save the tags on the note. Verify: creating a note with "personal, ideas" stores those tags correctly.
4. **Task 4 — Display tags**: Update the notes list to show tags as filter buttons next to each note title. Verify: notes display all their tags in the list view.
5. **Task 5 — Filter Notes by Tags**: Add a filter bar at the top with options like All, work, and personal, and filter notes based on the selected tag. Verify: clicking a tag only shows notes with that tag.
6. **Task 6 — Test Tags**: Add tests for creating tags, saving tags, and filtering notes. Verify: filters work correctly and all existing tests still pass.


## Task 1 context brief
Goal: Add a tags field to note object

Constraints:
- Do not make any changes to html.
- Keep using the current in-memory list of dictionaries structure.
- Do not work on filtering tags yet.

Files I expect to change:
- app.py

Acceptance:
- Newly created notes include tags: [].
- Existing notes without tags don't affect the app.
- Existing tests still pass

## Task 1 — what happened
- Claude's plan summary: 
Current:
app.notes.append({"title": title, "body": body, "user_id": current_user.id})
New:
app.notes.append({"title": title, "body": body, "user_id": current_user.id, "tags": []})
- What I pushed back on: app.notes.append({"title": title, "body": body, "user_id": current_user.id, "tags": []})
- Verification result: Passed tests

## Task 2 — what happened
- ...

## Task 3 — what happened
- ...

## Reflection — where I redirected
- (3 sentences)