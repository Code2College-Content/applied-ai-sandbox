# M5 — Task Decomposition

## Decomposition (8-10 tasks)
1. Extend the note dict shape to include a tags key that stores a list of strings, defaulting to [] for existing and newly created notes without tags. Verify: existing tests still pass and a newly created note contains tags == [].
2. Create a helper function to parse comma-separated tag input into a cleaned list of strings (trim whitespace, remove duplicates, ignore empty values). Verify: unit tests confirm "work, urgent , ,work" becomes ["work", "urgent"].
3. Update the new-note form template to include a text input for tags using comma-separated values. Verify: the form displays a tags field and submitting the form includes the tags data in the request.
4. Modify the /notes/new POST handler to call the tag parser and save parsed tags into the note dict. Verify: submitting "personal, school" creates a note with note["tags"] == ["personal", "school"].
5. Update the notes list template to render tags as chips next to each note title, while keeping notes without tags unchanged. Verify: tagged notes display chips and untagged notes still render normally.
6. Add logic to collect all unique tags from the in-memory notes list and render them in a filter bar at the top of the page, including an “All” option. Verify: the filter bar shows every unique tag exactly once.
7. Update the home/list route to read the ?tag= query parameter and filter notes to only those containing the selected tag; missing or empty tags should show all notes. Verify: visiting /?tag=work only displays notes whose tags list contains "work".
8. Add automated tests covering tag parsing, note creation with tags, tag chip rendering, and tag-based filtering behavior. Verify: full pytest suite passes and new tests confirm filtering and parsing work end-to-end.

## Task 1 context brief
Goal: Extend the in-memory note dict shape to support a tags key that stores a list of strings. Existing notes and newly created notes without tags should default to [].

Constraints:
- No database or new models; notes remain plain dicts.
- Keep changes minimal and backwards-compatible so existing behavior and tests still work.
- Do not implement parsing, UI, or filtering yet only the data shape/default handling.

## Task 1 — what happened
- Claude's plan summary: Added 1 line, removed 1 line
                return render_template("new_note.html", error
="Body is required",
                title=request.form.get("title"), body=request
.form.get("body"))
            # TASK 01 will add validation here.
            app.notes.append({"title": title, "body": body})
            app.notes.append({"title": title, "body": body, "
tags": []})
            return redirect(url_for("home"))
        return render_template("new_note.html")
- What I pushed back on: Nothing, it looked good
- Verification result: All 4 Task 01 tests pass ✅.

## Task 2 — what happened
- ...

## Task 3 — what happened
- ...

## Reflection — where I redirected
- (3 sentences)