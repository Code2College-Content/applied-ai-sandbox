# M5 — Task Decomposition

## Decomposition (6-10 tasks)
1. Implement a "tags" key to the note dict shape (Default = []). Verify: adding note produces app.notes[-1]["tags"' is [].
2. Integrate code which changes "work, urgent" into ["work", "urgent"], while discarding unnecessary text like whitespace. Verify: pytest case on the parser.
3. Include "tags" text input with new-note form template, while adding /note/new POST handler to call parser and save the list. Verify: "work, urgent" produces tags == ["work", "urgent"].
4. Template includes note title alongside tag chips. Verify: Old notes fold no chips if notes aren't tagged and show chips if notes are tagged.
5. Filter so each unique tag links to /?tag=<value>. Verify: "work" takes work-tagged notes and displays filtered version.
6. home() should read ?tag= from code and filter app.notes before processing. Verify: / & /?tag= show the same set.
7. Include pytest cases covering filter, parser, and save path. Verify: pytest passes.

## Task 1 context brief
<paste the full brief you'll give Claude>
Goal: Implement a "tags" key (list of strings) to the note dict shape.

Constraints: Only use change code from file <app.py>

Files I expect to change:
<app.py> in order to change the note app's interface.

Acceptance:
- Default to [] when key not provided
- adding note via form produces app.notes[-1]["tags"' is [].
- Previous tests pass alongside new

## Task 1 — what happened
- Claude's plan summary: Plan: Implement "tags" key on note dict shape
Context
This is Task 1 of the M5 milestone. The goal is to add a "tags" key to the note dict shape so subsequent tasks (parser, form input, filtering) have a stable foundation. Only app.py may be changed.

What changes
File: app.py — line 29

Current:

app.notes.append({"title": title, "body": body})
Change to:

app.notes.append({"title": title, "body": body, "tags": []})
That's the entire change. The tags value defaults to [] because no form input for tags exists yet — future tasks will populate it via a parser.

Why existing tests still pass
test_task_01.py — validates title/body presence and redirect; never inspects extra keys.
test_task_02.py — uses a _seed() helper that directly appends {"title": ..., "body": ...} (no tags), then checks title values and list length. Adding tags to the form-created path doesn't affect seeded notes or the assertions.
No test file is modified.


- What I pushed back on: Claude initially ignored the CLAUDE.md file and I ensured it used said file as the outline after back and forth with the program.

- Verification result: from app import create_app
app = create_app()
client = app.test_client()
client.post("/notes/new", data={"title": "T", "body": "B"})
assert app.notes[-1]["tags"] == []1

## Task 2 — what happened
- Claue added an in code helper which parses strings. A better understanding of what the task at hand was regarding how the code should coincide alongside other tasks like the tag key was asked.

## Task 3 — what happened
- ...

## Reflection — where I redirected
- (3 sentences)