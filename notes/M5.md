## Decomposition
Add tags field to note dict → verify new note has empty tags
Update note creation to accept tags input → verify tags saved correctly
Parse comma-separated tags → verify split works
Update list view to display tags → verify UI shows chips
Add filter state (selected tag) → verify state changes on click
Filter notes by tag → verify only matching notes show
Add “All” reset filter → verify full list returns
Add/adjust tests → verify pytest passes

## Task 1 context brief
Goal: add tags field to note dict
Constraints: in-memory dict only, no DB
Files: app.py, maybe templates/home.html
Acceptance: note has "tags": [], no test break

# Task 1 - what happened
Added a tags field to notes and implemented Task 01 form validation — both complete and tested. Next: implement Task 02 (the delete route). (disable recaps in /config)

# Task 2 - what happened
Claude added tags field correctly
I pushed back on storing tags as string instead of list
Tests passed after fix

# Reflection
The first plan came back slightly wrong because Claude wanted to overcomplicate the tags feature instead of keeping it as a simple list on the note dict. For task 2, I made the brief more explicit about staying in-memory only and avoiding new models or databases. If I re-did the decomposition, I would split the UI filtering work into even smaller tasks so verification would be faster.