# M5 — Task Decomposition

## Decomposition (8-10 tasks)
1. Extend the note dict shape to include a tags key with a default empty list ([]) for all notes. Verify: existing tests still pass and newly created notes always contain tags == [].
2. Update note creation logic to normalize tags input into a list of trimmed strings. Verify: input "work, urgent" becomes ["work", "urgent"] and extra spaces are removed.
3. Add a tags input field to the new-note form using comma-separated text entry. Verify: creating a note through the UI stores tags correctly in the in-memory note dict.
4. Ensure notes without tags still render correctly and do not break existing list/detail views. Verify: old notes with no tags display normally and app does not crash on missing/empty tags.
5. Update the notes list view to render tags as visual chips next to each note title. Verify: notes with tags show chips like work and urgent; notes without tags show no chips.
6. Add logic to compute the unique set of tags across all notes for the filter bar. Verify: duplicate tags only appear once in the filter list and all existing tags are included.
7. Implement the filter bar UI with an "All" option plus one button/chip per unique tag. Verify: all tags appear dynamically after notes are added.
8. Add filtering behavior so selecting a tag only displays notes containing that tag. Verify: selecting "work" hides notes without the "work" tag, while "All" restores the full list.
9. Add tests for tag parsing and normalization behavior. Verify: tests confirm comma splitting, trimming whitespace, ignoring empty entries, and default empty tags.
10. Add tests for filter behavior in the notes list view. Verify: tests confirm "All" shows every note and selecting a tag only shows matching notes while existing tests continue passing.

## Task 1 context brief
Goal: Extend the in-memory note dict shape to support tags by adding a tags key that defaults to an empty list on all notes.

Constraints:

Notes are plain Python dicts, not models/dataclasses.
Do not implement UI or filtering yet.
Preserve compatibility with existing notes/tests.
tags must always be a list of strings.
Keep changes minimal and localized.

Files I expect to change:

Note creation/storage logic file(s)
Existing note fixtures or seed data if required
Relevant test files for note creation/default shape

Acceptance:

Newly created notes include {"tags": []} by default.
Existing functionality and tests still pass unchanged.
No crashes when older notes are missing the tags key.
Add/update tests verifying the default tags behavior.

Process:

Start in plan mode first.
Before editing, identify where notes are instantiated and where tests cover note structure.
After the plan, make the smallest possible implementation change and run tests.

## Task 1 — what happened
Claude's plan summary:
Claude explored the codebase to find where notes were instantiated and where tests validated note structure. It identified that the tags field had already been added to new note creation in app.py and that tests/test_tags.py already covered both default tag behavior and backward compatibility for older notes missing the tags key. The plan concluded that no additional implementation work was needed besides running the test suite and verifying acceptance criteria.
What I pushed back on:
I pushed back on keeping unrelated changes bundled with the task. Claude noticed dark-mode template edits and other unrelated files in the diff, then scoped the commit to only the tags-related files (app.py and tests/test_tags.py) before later making a separate commit for the remaining session changes.
Verification result:
Ran the test suite. All tags-related tests passed:
New notes now include "tags": [] by default.
Older notes without a tags key do not crash when accessed safely with .get("tags", []).
Existing note functionality remained compatible with prior tests.
The only failing tests were unrelated pre-existing exercises (test_task_02 delete-route work and test_bughunt_03).cal

## Task 2 — what happened
Claude's plan summary:
The goal was to implement tag parsing in the note creation POST handler. The plan identified that request.form.get("tags", "") should be parsed into a normalized list. It specified splitting on commas, stripping whitespace, and removing empty entries so inputs like "work, urgent" become ["work", "urgent"]. It also required adding unit tests for splitting, trimming, empty values, and ensuring "" returns [], while preserving backward compatibility for existing notes.
What I pushed back on:
No major pushback on logic, but the implementation showed some over-editing risk (duplicate or malformed app.notes.append lines in the diff). The key implicit constraint I enforced was keeping the change minimal and ensuring the tags logic is the only functional modification in app.py, without introducing structural drift or duplicate append calls.
Verification result:
All 9 tests passed after running the full suite.
Tag parsing works correctly ("work, urgent" → ["work", "urgent"])
Whitespace is trimmed and empty entries are removed
Empty string produces []
Existing tests remain unaffected
Legacy note safety (get("tags", [])) still holds
No regressions were introduced.

## Task 3 — what happened
- new_note form: tags input (comma-separated); raw_tags kept separate
  from parsed list so form refill and storage never mix
- tags normalized to lowercase at write time; filter query also
  lowercased so exact-match is safe_
- home page: filter bar with All + unique tag links (server-side
  ?tag= query param); chips on note cards are display-only spans
- 4 new tests: chip rendering, filter-bar deduplication (regex-scoped),
  per-tag hiding, and All-restores-full-list

  What I pushed back on: Claude had decided it wanted to move the raw tags early. This wasn't a very secure method of doing this task, so I asked claude to rethink and change the way it was working on that part.

  It passed all of the tests it ran, and some manual verification tasks as well.

## Reflection — where I redirected
- (3 sentences)