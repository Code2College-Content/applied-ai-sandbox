# M5 - Task Decomposition
1. Task 1 - Include tags to the new data structure (new key 'tags" is a list of strings where the default is []). Verify: Existing tests still pass, a new note named tags is == []
2. Task 2 - Update note creation logic allowing for commas to be accepted and converted. Verify: Existing tests still pass, and inputs -- such as "candy, drinks", -- becomes note["tag"] == ["candy", "drinks"]
3. Task 3 - Handle undefined tags. Verify: Existing tests still pass, and ensures old notes defaults to an empty list instead of breaking.
4. Task 4 - Update the UI to include a tags input field. Verify: Existing tests still pass, and UI shows tag fields and notes, including tags, are stored into data.
5. Task 5 - Display tags as chips. Verify: Existing tests still pass, and visually display note tages as chips.
6. Task 6 - Add a filter bar for tags. Verify: Existing tests pass, and that there is a clear tag filter bar with options that if selected, displays notes with that tag.
7. Task 7 - Filter notes based on tasks. Verify: Existing tests pass, and filtering works properly without issues.

# Task 1 context brief
Goal: New database in notes where  note["tag"] == []

Constraints:
- Previous notes already made
- Make sure tag is in note and not any other database
- Tags must always default to an empty database

Files I expect to change:
- note - I want to add "tags" to its structure

Acceptance:
- New notes created includes tags == []
- Existing codes without tags do not crash
- Existing tests still pass.

First: read the relevant files and propose a plan in plan mode.
Don't write code yet.

# Task 1 - What happened
Progress update: added tags: [] to new notes, updated the todo list, and ran tests