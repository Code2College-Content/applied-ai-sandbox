# M5 — Notes

_Cohort notes for the M5 module. Edited as I work through it._

---

## Task 1 — Add tags key to note dicts

**Claude's plan:** One line change in `app.py` — add `"tags": []` to the `app.notes.append(...)` call so every new note has the field from creation. No templates, no other routes touched.

**What I pushed back on:** Nothing — the plan was minimal and matched the constraint exactly. The brief ("don't change anything else") kept Claude from expanding scope into form inputs or filtering.

**Verification:** `pytest` — `test_empty_tags_saves_empty_list` passes, confirming the key exists and defaults to `[]`. Other failures are pre-existing (Task 01/02 not yet implemented on `main`; M5 tag tests require the full feature from future tasks).

**Commit:** `m5-task-1-tags-key`
