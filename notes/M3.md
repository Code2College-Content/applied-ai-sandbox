# M3 — Context Engineering

_Cohort notes for the M3 module. Edited as I work through it._


# 2

Run pytest after changes. Don't edit the tests to make them pass. app.py is the server entry point.

# 3 Bad Context

- Assumed: Use of flask, app.py for logic, new login template, login/logout buttons, username and password
- Wrong: Login has no use and does not check if user is logged in to use their own notes
- Asked: nothing

# 4

Planning File Review

First, I'm reminded to read relevant files before planning. The to-do list tool is essential for tracking progress. I need to check files like app.py, templates, tests, and requirements.txt to ensure Flask-Login is present. So, I'll read app.py and requirements.txt first. There are some tests to verify for tasks 1 and 2, and I need to confirm they pass after any changes. Then, I will propose a plan using the to-do tool.

Organizing Todo Steps

I need to set up a to-do list for planning steps, but first, the user wants me to read relevant files and propose a plan without writing any code. So, I’ll start by reading the necessary files, then I’ll use the manage_todo_list tool to create a high-quality plan with clear steps. I’ll preface my tool calls by explaining that I will read key files to gather the current app structure, then call read_file multiple times. I'll check files like app.py, templates, and requirements.txt.

Preparing for Tool Calls

I need to send a brief preamble before making tool calls as per the instructions. This will be a single sentence for grouping actions together. After the preamble, I'll do multiple read_file calls sequentially. First, I'll read app.py, then requirements.txt, and since there’s no models.py, I'll read test_task_01.py and test_task_02.py. I’ll also check pyproject.toml along with templates like home.html and new_note.html. After reading everything, I’ll propose a plan and create a to-do list.

# 5.

1. "Logged-in users see their own notes only."

2. None

3. Create tests for login/logout