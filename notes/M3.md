# M3 — Context Engineering

_Cohort notes for the M3 module. Edited as I work through it._

## Step 2 — CLAUDE.md behaviors

- It tells Claude to read the task file before writing code, so Claude doesn't start implementing based on assumptions and miss what the task actually asks for.
- It explicitly says don't edit the tests to make them pass, so Claude doesn't take the shortcut of changing it instead of fixing the code.
- It tells Claude to plan before implementing, so Claude doesn't dive straight into writing code and skip the step where the human can catch a wrong approach early.

